
name: docker-picrin-push-action

on:
  push:
    branches:
      - 'master'
    paths:
      - implementations/picrin/**/Dockerfile

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Build and push schemers/picrin latest
        run: make VERSION=latest SCHEME=picrin build push
      -
        name: Build and push schemers/picrin head
        run: make VERSION=head SCHEME=picrin build push
