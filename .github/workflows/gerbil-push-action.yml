
name: docker-gerbil-push-action

on:
  push:
    branches:
      - 'master'
    paths:
      - implementations/gerbil/**/Dockerfile

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Build and push schemers/gerbil:0
        uses: docker/build-push-action@v2
        with:
          push: true
          context: implementations/gerbil/0/
          tags: |
            schemers/gerbil:0
            schemers/gerbil:0-bullseye-slim
            schemers/gerbil:latest
      -
        name: Build and push schemers/gerbil:head
        uses: docker/build-push-action@v2
        with:
          push: true
          context: implementations/gerbil/head/
          tags: |
            schemers/gerbil:head
            schemers/gerbil:head-bullseye-slim
