
name: docker-vicare-push-action

on:
  push:
    branches:
      - 'master'
    paths:
      - implementations/vicare/**/Dockerfile

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Build and push schemers/vicare latest
        run: make VERSION=latest SCHEME=vicare build push
      -
        name: Build and push schemers/vicare head
        run: make VERSION=head SCHEME=vicare build push
