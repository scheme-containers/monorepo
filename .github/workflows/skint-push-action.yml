
name: docker-skint-push-action

on:
  push:
    branches:
      - 'master'
    paths:
      - implementations/skint/**/Dockerfile
  schedule:
    - cron: "17 4 * * 2"

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Build and push schemers/skint latest
        run: make VERSION=latest SCHEME=skint build push
      -
        name: Build and push schemers/skint head
        run: make VERSION=head SCHEME=skint build push
