
name: docker-capyscheme-push-action

on:
  push:
    branches:
      - 'master'
    paths:
      - implementations/capyscheme/**/Dockerfile
  schedule:
    - cron: "17 4 * * 2"

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Build and push schemers/capyscheme latest
        run: make VERSION=latest SCHEME=capyscheme build push
      -
        name: Build and push schemers/capyscheme head
        run: make VERSION=head SCHEME=capyscheme build push
