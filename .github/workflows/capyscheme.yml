name: capyscheme

on:
  workflow_dispatch:
  push:
    paths:
      - implementations/capyscheme/**/Dockerfile*
      - .github/workflows/capyscheme*.yml
jobs:
  latest:
    strategy:
      fail-fast: false
      matrix:
        linux: [debian, alpine]
        os: [ubuntu-24.04, ubuntu-24.04-arm]
        version: [latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Build capyscheme
        if: github.ref != 'refs/heads/master'
        run: make VERSION=${{ matrix.version }} SCHEME=capyscheme LINUX=${{ matrix.linux }} build
      - name: Login to DockerHub
        if: github.ref == 'refs/heads/master'
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and push capyscheme
        if: github.ref == 'refs/heads/master'
        run: make VERSION=${{ matrix.version }} SCHEME=capyscheme LINUX=${{ matrix.linux }} build push
